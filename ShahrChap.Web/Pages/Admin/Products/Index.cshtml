@page
@model ShahrChap.Web.Pages.Admin.Products.IndexModel
@{
    ViewData["Title"] = "لیست محصولات";
}
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">لیست محصولات سایت</h1>
    </div>
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-body">
                <div class="table-responsive">
                    <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">
                        @* <div class="row" style="margin-bottom:.5rem">
                            <form>

                                <div class="col-sm-5 col-md-5">
                                    <input type="text" name="filterUser" class="form-control" placeholder="جستجو..." />
                                </div>
                                <div class="col-sm-4 col-md-4">
                                    <button type="submit" class="btn btn btn-success"><i class="uil uil-search"></i></button>
                                    <a href="/Admin/Users/" class="btn btn btn-outline btn-danger"><i class="uil uil-times"></i></a>
                                </div>
                            </form>
                        </div> *@
                        <div class="row">
                            <div class="col-md-12" style="margin-bottom:1rem">
                                <a asp-page="CreateProduct" class="btn btn-outline btn-success"><i class="uil uil-archive"></i> افزودن محصول جدید</a>
                            </div>
                        </div>
                        <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info" style="text-align:center;vertical-align:central;">
                            <thead>
                                <tr>
                                    <th>تصویر</th>
                                    <th>عنوان دوره</th>
                                    <th>دستورات</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.ProductList)
                                {
                                    <tr>
                                        <td>
                                            <img src="/product/thumb/@item.ImageName" class="thumbnail" />
                                        </td>
                                        <td>@item.ProductTitle</td>
                                        <td>
                                            <a asp-page="ProductFeatures" asp-route-id="@item.ProductId" class="btn btn-sm btn-info"><i class="uil uil-label-alt"></i></a>
                                            <a asp-page="ProductServices" asp-route-id="@item.ProductId" class="btn btn-sm btn-info"><i class="uil uil-plus-circle"></i></a>
                                            <a asp-page="EditProduct" asp-route-id="@item.ProductId" class="btn btn-sm btn-warning"><i class="uil uil-edit"></i></a>
                                            <a asp-page="DeleteProduct" asp-route-id="@item.ProductId" class="btn btn-sm btn-danger"><i class="uil uil-trash-alt"></i></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        @* <div class="row">
                            <div class="col-sm-6">
                                <div class="dataTables_paginate paging_simple_numbers" id="dataTables-example_paginate">
                                    <ul class="pagination">
                                        @for (int i = 1; i <= Model.UserForAdminViewModel.PageCount; i++)
                                        {
                                            <li class="paginate_button @((i==Model.UserForAdminViewModel.CurrentPage)?"active":"")" aria-controls="dataTables-example" tabindex="0">
                                                <a href="/Admin/Users?PageId=@i">@i</a>
                                            </li>
                                        }
                                    </ul>
                                </div>
                            </div>
                        </div> *@
                    </div>
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>
</div>